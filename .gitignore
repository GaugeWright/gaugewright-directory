# Rust build output (also holds the mkdocs site build, target/directory-docs-site)
/target/

# Claude Code local agent config / state
.claude/

# Local environment and secret material. The baseline scanner answers for what
# already landed; this is what keeps it from landing.
.env*
!.env.example
*.pem
*.key
